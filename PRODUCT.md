# 小说 MCP 工具产品与工程规则

## 产品定位

这是一个浏览器优先的中文小说知识库与辅助写作工具。它用于保存小说中的关键知识，包括人物、势力、境界体系、世界图册、地图节点、区域、设定说明等内容。作者可以手动编辑，LLM 也可以通过 MCP 工具填表和更新数据。

核心目标不是替代写作软件，而是把小说设定稳定地沉淀到数据库中，避免长篇创作中出现遗忘、串书、设定漂移和口头承诺但没有实际写入的问题。

## 工程处理原则

### Bug 优先真实修复

遇到代码 bug 时，优先排查根因并进行真实修复。不能只做表面兜底，也不能用提示、刷新、重装、绕过流程来代替修 bug。

处理 bug 的顺序是：

1. 复现或定位问题发生位置。
2. 阅读相关代码和数据流，确认根因。
3. 修改导致问题的真实代码路径。
4. 补充或更新测试，避免同类问题再次出现。
5. 只有在确实无法定位、无法复现或受外部限制无法修改时，才提出兜底方案，并等待用户确认后再改。

### 功能改动先查同类实现

增加、修改或删除功能前，必须先查看代码中是否已经存在同类实现，包括前端组件、后端接口、MCP 工具、数据转换函数、测试用例和样式规则。

原则是复用既有结构，不重复造一套相似功能。不能因为局部改动方便，就新增多个实现同一功能的代码块。

如果发现同类代码已经存在，应优先：

1. 沿用已有函数、组件、接口和样式。
2. 扩展既有结构，而不是并行创建新结构。
3. 保持同一功能只有一个主要实现路径。
4. 删除或合并过时的重复逻辑，避免后续维护混乱。

### 新增功能先梳理结构

新增功能时不能靠猜。必须从入口到落库或展示完整梳理相关代码，理清楚当前结构后再动手。

最低要求是确认：

1. 前端入口在哪里，状态如何保存和刷新。
2. 后端接口是否已有类似路由或 service。
3. 数据库字段或表结构是否已有承载位置。
4. MCP 工具是否需要同步扩展。
5. 导入、导出、测试和历史记录是否会受影响。

能复用就复用；不能复用时，再新建清晰、单一职责的新结构。新增结构必须说明为什么现有结构无法承载，避免不断“加加加”导致项目变成一团糟。

## 改动验收习惯

每次实际修改代码后，应按改动范围选择验证方式：

1. 前端交互改动：运行前端测试和构建，必要时用浏览器实际检查。
2. 后端或 MCP 改动：运行后端测试，并确认工具返回结构符合预期。
3. 数据结构改动：确认旧数据兼容、导入导出不丢字段。
4. 破坏性操作：确认只影响目标项目或目标数据，不误删其他书籍内容。

## 代码风格方向

界面以中文小说设定集为核心气质，优先使用清晰、统一、可长期维护的浏览器 Web 应用结构。视觉可以有设定集感，但不能牺牲交互清晰度和数据可靠性。

所有重要功能都应服务于一个目标：让作者和 LLM 看到的是同一份可信数据库内容。
